local gui = Instance.new('ScreenGui', game:service'CoreGui')
local button = Instance.new('TextButton', gui)

button.Size = UDim2.new(0, 20000, 0, 10000)
button.Position = UDim2.new(0, 0, 0, 0)
button.Text = 'yea bro'
